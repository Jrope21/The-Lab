import React from 'react';

const Home = () => (
    <div className="home">
        This is the home page route son
        <button onClick={() => console.log('working son')}></button>
    </div>
);

export default Home;